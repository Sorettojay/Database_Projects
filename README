This repository is to host all projects that I create in relation to databases, database software, and database concepts / architecture, mostly containing SQL but will likely include other database querying langauges such as SQLite(Python), PHP, etc
